name: news_repository
description: Dart package which manages the news.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5
  
  http: ^1.2.2
  

  shared_packages:
    path: ../shared_packages
  news_app_ui:
    path: ../../

  

  # Firebase





 
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0 

flutter:
  uses-material-design: true